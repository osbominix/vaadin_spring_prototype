# vaadin_spring_prototype
use push in vaadin spring

This is a maven project with vaadin and spring.

By using server push on the UI class, current time will be shown as label caption second by second.

http://localhost:8080